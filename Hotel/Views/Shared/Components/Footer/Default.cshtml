@model List<Footer>

<div class="row pb-50">
	@foreach (var footer in Model)
   {
	   <div class="col-lg-3 col-sm-6">
				<div class="single-footer-widget">
					<h5>@footer.Title</h5>
					<div class="widget-text">
						<i class="fa fa-map-marker"></i>
						<p>@footer.Decription</p>
					</div>
				</div>
			</div>
						
   }
		</div>

			